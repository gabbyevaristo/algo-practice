# Properties
"""
    - Every node has a color: either red or black (data attribute)
    - Root of the tree is always black
    - There are no two adjacent red nodes (a red node cannot have a red parent or child)
    - Every path from a node (including the root) to any of its descendant NULL node has
      the same number of black nodes
"""

# Recoloring/rotations are performed if they become imbalanced



# Splay tree
""" Fast for searching by placing recently accessed item to the root, giving it an O(1)
search complexity if searched again """


# Other self-balacing BST trees include:
"""
    1. Scapegoat trees (height can differ by a certain alpha)
    2. B-trees
"""
